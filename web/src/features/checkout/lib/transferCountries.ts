// Fallback country list for transfer products whose inputFields don't define a
// `country` select. When the product schema provides a select, its
// constraints.options are used instead (server-derived).
export const TRANSFER_COUNTRIES = [
  'Türkiye',
  'United States',
  'United Arab Emirates',
  'Saudi Arabia',
  'Egypt',
]
