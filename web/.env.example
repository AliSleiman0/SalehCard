# Must match the API's PORT. Note: api/.env.example defaults PORT=8080 but its
# PUBLIC_BASE_URL (used in image URLs) assumes :8090 — set both consistently or
# product/KYC images will 404. Local convention: run the API on 8090.
VITE_API_BASE_URL=http://localhost:8090
VITE_GOOGLE_CLIENT_ID=
